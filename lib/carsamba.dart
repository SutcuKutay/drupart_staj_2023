import 'package:flutter/material.dart';
import 'screens/tabsScreen.dart';

class Carsamba extends StatelessWidget {
  const Carsamba({super.key});

  @override
  Widget build(BuildContext context) {
    return const TabsScreen();
  }
}
